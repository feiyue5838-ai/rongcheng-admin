# 蓉城企服管理后台前端修复方案
（配套《前端审计报告_2026-08-26_v2.md》，不改动现有代码，供评审后实施）

- **日期**：2026-08-26
- **适用范围**：报告内全部 P0/P1/P2 问题 + 系统性改造项
- **约定**：文中行号以审计当日工作区代码为准；示例代码需按实际文件上下文微调；`request` 指 `src/api/index.ts` 的 V1 实例（已自动解 `{code:0,data}` 单层包装），`v2Request` 指 V2 双层解包实例。
- **验证方式**：每项修复后跑 `npx vue-tsc --noEmit`；全部完成后 `npm run build` + 按第五节回归清单手工验证。

---

## 一、P0 修复方案（10 项）

### P0-1 订单分配网点列表恒为空（派单不可用）
**位置**：`views/outlets/OrderAssign.vue:359`、`views/orders/bookkeeping.vue:274`
**根因**：`/dispatch/outlets/available` 后端返回裸数组（`DispatchService.getAvailableOutlets` 返回 `List`），经拦截器解包后 `res` 即数组；`(res as any).data ?? []` 恒取到 `[]`。
**推荐做法（集中修复，治本）**：在 `src/api/index.ts` 把接口封装为"已归一化为数组"：

```ts
// api/index.ts 替换原 getAvailableOutlets
export const getAvailableOutlets = async (params?: { addressJson?: string; businessType?: string }) => {
  const res: any = await request.get('/dispatch/outlets/available', { params })
  return Array.isArray(res) ? res : ((res as any)?.list ?? [])
}
```

**调用方同步简化**：
- `OrderAssign.vue:358-359`：
  ```ts
  const all = await getAvailableOutlets({ addressJson: order.addressJson, businessType: order.module || 'seal' })
  ```
- `bookkeeping.vue:273-274`：
  ```ts
  outletList.value = await getAvailableOutlets(params)   // 外面已有 try/catch
  ```
- `seal.vue:360-362`（现写法 `(res as any).data ?? res` + `Array.isArray` 校验可保留，也可一并简化）

**验证**：进入 `/outlets/assign` 打开分配弹窗，应有网点列表；记账订单分配弹窗同理。

### P0-2 TrendChart 趋势图首次加载永不渲染
**位置**：`components/TrendChart.vue:12-15 / 78-118 / 190-192`
**根因**：图表容器 `v-else` 只在数据非空时渲染，`onMounted` 时 `chartRef` 为 null → 初始化中止；数据到达后 watch 只调 `renderChart()`，因 `chartInstance` 为 null 直接 return，无人再调 `initChart()`。
**改法（最小改动）**：watch 中数据由空转非空时补一次初始化：

```ts
watch(() => props.data, async (val) => {
  if (val?.length && !chartInstance) {
    await nextTick()
    await initChart()
    return
  }
  nextTick(() => renderChart())
}, { deep: true })
```

**改法（推荐，更稳）**：去掉 `v-else`，让容器常驻并叠加空态提示（与 `ModuleDonut.vue` 一致）：

```html
<div ref="chartRef" class="echarts-container"></div>
<div v-if="trendEmpty" class="empty-mask">暂无趋势数据</div>
```
```css
.echarts-container { position: relative; }
.empty-mask { position: absolute; inset: 0; display: flex; align-items: center; justify-content: center; color: #909399; }
```
同时删除 13 处 `console.log`（64/69/79/81/84/90/95/103/112/116/179/181/200 行）。

**验证**：财务总览首次进入（数据异步加载）趋势图可见；切"近30天/近90天"图表刷新。

### P0-3 登录态 localStorage 损坏导致整站白屏
**位置**：`src/stores/auth.ts:10-12`
**改法**：解析加 try/catch：

```ts
function safeParseAdminInfo(): any {
  const raw = localStorage.getItem('admin_info')
  if (!raw) return null
  try { return JSON.parse(raw) } catch { return null }
}
const adminInfo = ref<any>(safeParseAdminInfo())
```
顺带清理死代码 `fetchAdminInfo`（45-49 行，从未被调用；如需"刷新资料"功能请接入调用点）。

**验证**：手动把 `localStorage.admin_info` 改为损坏 JSON 后刷新页面，应回退为未登录而非白屏。

### P0-4 内容管理富文本存储型 XSS
**位置**：`views/content/index.vue:733-743`（`normalizeRichText`）、`:363`（工具栏配置）
**改法**：
1) 安装净化库：`npm i dompurify`（如需类型：`npm i -D @types/dompurify`）
2) 保存前白名单净化：
```ts
import DOMPurify from 'dompurify'

function normalizeRichText(value: string) {
  const html = value.trim()
  if (!html) return ''
  return DOMPurify.sanitize(html, {
    USE_PROFILES: { html: true },
    FORBID_TAGS: ['script', 'iframe', 'object', 'embed', 'form', 'style'],
    FORBID_ATTR: ['onerror', 'onload', 'onclick', 'style'],   // 按需保留 style
  })
}
```
3) 禁用 wangEditor 源码编辑入口（当前 `excludeKeys: []` 未禁）：
```ts
const editorToolbarConfig = { excludeKeys: ['editHtml'] }
```
4) 渲染侧：凡把 `termsContent/privacyContent/materialCommitment` 渲染为 HTML 的地方（管理端预览、小程序 rich-text/web-view）保持白名单/转义，不在富文本字段上使用未净化输出。
5) 后端 `ContentService` 保存接口建议加同样过滤（纵深防御）。

**验证**：在公告/关于我们富文本源码模式粘贴 `<img src=x onerror=alert(1)>` 或 `<script>alert(1)</script>` 保存，重新加载后应被剥离；正常图片/表格保留。

### P0-5 历史订单页管理操作无入口 + 记账页缺分页
**位置**：`views/orders/seal.vue:92-96`、`newspaper.vue:88`、`bookkeeping.vue:107`、`views/after-sales/after-sales-orders.vue:89-93`
**改法（按状态机补按钮）**，以 `seal.vue` 为例：

```html
<el-table-column label="操作" width="200" fixed="right">
  <template #default="{ row }">
    <el-button type="primary" link @click="$router.push(`/orders/${row.id}`)">详情</el-button>
    <el-button v-if="row.status === 1" type="primary" link @click="showAssignDialog(row)">分配</el-button>
    <el-button v-if="row.status === 2" type="primary" link @click="showDeliverDialog(row)">发货</el-button>
    <el-button v-if="row.status === 2" type="warning" link @click="handleStatus(row, 4, '已发货')">标记发货</el-button>
  </template>
</el-table-column>
```
> 按钮显示条件请按各页实际状态机（1 待支付/待分配 → 2 已支付/制作中 → 4 已发货 → 5 已完成）与后端 `OrderService` 状态常量核对后确定；`newspaper.vue`/`bookkeeping.vue` 的 `showAssignDialog`/`showDeliverDialog` 等函数已存在，直接接按钮即可。`after-sales-orders.vue` 补"确认退款/拒绝售后"按钮（已有 `confirmRefund(row)`/`openReject(row)`）。

**记账页补分页**（`bookkeeping.vue`，`query.page/pageSize` 与 `total` 已存在）：

```html
<el-pagination
  style="margin-top: 20px; justify-content: flex-end"
  v-model:current-page="query.page"
  v-model:page-size="query.pageSize"
  :total="total"
  :page-sizes="[10, 20, 50, 100]"
  layout="total, sizes, prev, pager, next"
  @change="fetchOrders"
/>
```

**备选**：若产品确认这些历史页只读，则删除对应对话框与函数（避免死代码），并去掉误导性的"只读"文案。

**验证**：刻章/登报/记账列表可对状态 1 订单分配、状态 2 订单发货；记账页可翻页到第 2 页；售后页可发起退款/拒绝。

### P0-6 订单详情页"发货"双重缺陷（被硬编码只读掩盖）
**位置**：`views/orders/detail.vue:324`（`legacyReadOnly = true`）、`:362-396`（`confirmDeliver`）
**改法**（若页面维持只读）：删除 157-199 管理卡片与 204-294 四个对话框及相关函数（`saveChanges/confirmRefund/confirmAfterSales/confirmDeliver/doAuditMaterial/showAssignDialog`），`legacyReadOnly` 相关判断一并清理。
**改法**（若需启用管理操作）：
1) `api/index.ts` 新增专用发货接口（对齐后端 `PUT /orders/{id}/deliver-admin`，snake_case）：
```ts
export const deliverOrderAdmin = (id: string, data: {
  express_company: string; express_no: string; remark?: string
  receipts?: Array<{ type: string; url: string }>
}) => request.put(`/orders/${id}/deliver-admin`, data)
```
2) `confirmDeliver` 改为单次调用、删除原生 fetch 与 `updateOrder` 双写：
```ts
await deliverOrderAdmin(order.value.id, {
  express_company: deliverForm.value.expressCompany,
  express_no: deliverForm.value.expressNo.trim(),
  remark: deliverForm.value.remark,
  receipts: deliverFileList.value.map(f => ({ type: 'certificate', url: f.url })),
})
```
3) 逐项复核其余对话框（退款/售后/分配）的字段契约后再放开 `legacyReadOnly = false`。
4) 材料审核默认值：`:587` `m._auditStatus = 1` 改为 `m._auditStatus = m.status`（保持"待审核"不预选"通过"）。

**验证**：详情页发货后快递信息入库（后端日志/DB 可查 `express_company`）；无 401；无重复发货。

### P0-7 交易流水页"导出"未实现 + "查看订单详情"跳转失效
**位置**：`views/transaction/index.vue:424-440`、`:452`
**改法 1（跳转）**：
```ts
// 原：window.open(`/#/orders/detail/${orderId}`, '_blank')
window.open(`/orders/${orderId}`, '_blank')   // 或 router.push(`/orders/${orderId}`)
```
**改法 2（导出）**：
- 方案 A（推荐，后端配合）：`api/index.ts` 参照 `exportOrders` 增加 blob 下载接口，后端 `/transaction/export` 改为返回 `ResponseEntity<byte[]>`（xlsx，参照 `OrderController.exportOrders`）：
```ts
export const exportTransactionFlows = (params?: any): Promise<Blob> =>
  request.get('/transaction/export', { params, responseType: 'blob', timeout: 60000 }) as unknown as Promise<Blob>
```
  前端 `onExport`：
```ts
async function onExport() {
  exporting.value = true
  try {
    const blob = await exportTransactionFlows({ ...筛选参数 })
    const url = URL.createObjectURL(blob)
    const a = document.createElement('a')
    a.href = url; a.download = '交易流水.xlsx'; a.click()
    URL.revokeObjectURL(url)
    ElMessage.success('导出成功')
  } catch { ElMessage.error('导出失败') } finally { exporting.value = false }
}
```
- 方案 B（不改后端）：后端返回 `{data:[...], filename}`，前端用 CSV 客户端下载（复用 `finance/index.vue` 的 `downloadCSV` 模式，带 BOM 供 Excel 打开），导出即生效。

**验证**：交易流水页点"导出"能下载可打开的文件；点"关联订单"跳转到对应订单详情页。

### P0-8 退款管理（V1）页面：无路由死代码 + 解包错误
**位置**：`views/refund/index.vue`（整页）
**建议**：直接删除该文件（已被 `/v2/refunds` 取代），同时删除 `api/index.ts` 中仅它使用的 `getRefundList/applyRefund/reviewRefund/executeRefund`（285-288 行，确认无其他引用后）。
**若保留**：修正解包与跳转：
```ts
list.value = res.items ?? res.data?.items ?? []
total.value = res.total ?? res.data?.total ?? 0
// viewOrder 改为跳转真实详情：
router.push(`/orders/${row.orderId}`)   // 使用订单号而非数字主键，并确认字段名
```

**验证**：`/refund`、`/finance/refund` 均 302 到 `/v2/refunds`；删除后无残留 import 报错。

### P0-9 网点结算周期字段名与后端不符（功能失效）
**位置**：`views/outlets/OutletList.vue:808-813`
**根因**：后端 `OutletService.create/update`（230/302 行）只读 `settlementCycle`/`settlementWeeklyStartDay`（camelCase）。
**改法**：
```ts
if (form.settlementCycle != null && form.settlementCycle !== '') {
  payload.settlementCycle = form.settlementCycle
  payload.settlementWeeklyStartDay = form.settlementCycle === 'weekly' ? form.settlementWeeklyStartDay : 1
} else {
  payload.settlementCycle = null   // 后端 containsKey 判断，允许置 null 关闭自动结算
}
```
同时处理 `el-select clearable` 清空后 `''` 与 `null` 的边界（`222-227` 行的回显判断建议统一 `form.settlementCycle || null`）。

**验证**：新增/编辑网点选择"周结 + 每周几"保存后，重新打开编辑弹窗回显正确；后端 DB `settlement_cycle` 有值。

### P0-10 全网点总览页订单统计字段名与后端不符（且后端为占位值）
**位置**：`views/outlets/OutletOverview.vue:40/73/130/182/225-248/305`
**改法（前端）**：统一归一化取值，兼容两套命名：
```ts
const n = (v: any) => v?.total_orders ?? v?.totalOrders ?? 0
// summary.total_orders → n(summary)；regions[].total_orders → n(r)；outlets[].total_orders → n(o)
```
**改法（后端，必须）**：`OutletService.getOverview()`（503/518/540 行）当前 `total_orders` 等为**硬编码占位 0**，需补真实聚合：按 region/outlet 统计 `assignment` 表的订单数、pending（状态 1）、processing（2）、completed（3）、today（当日 created_at）。建议同时统一后端两套命名（`/outlets` 列表用 camelCase `totalOrders`，overview 用 snake_case `total_orders`）。

**验证**：总览页四个 KPI、柱状图、两表格"累计"列有真实数据；与网点列表口径一致。

---

## 二、P1 修复方案

### P1-1 网点工作台 3 个功能缺陷（`views/outlet/Workspace.vue`）
1) 业务类型空配置整页置空（461-468）：
```ts
function getAuthorizedOrders(allOrders) {
  const codes = outletStore.outletInfo?.businessTypes?.map(b => b.code) ?? []
  if (!codes.length) {
    console.warn('[Workspace] 网点未配置业务类型，展示全部订单')
    return allOrders
  }
  return allOrders.filter(o => {
    if (!o.orderItems || o.orderItems.length === 0) return true
    return o.orderItems.some(item => codes.includes(item.itemType))
  })
}
```
2) 绝对地址拼接坏链接（407-409）：`window.open(getMaterialUrl(url), '_blank')`（`getMaterialUrl` 412-413 已存在）。
3) 100 条截断（473/483/494）：接入真实分页（`page/pageSize` 状态 + `el-pagination` 调用 `loadData`），或在数据超限时提示"仅显示最近 100 条"。

### P1-2 网点看板状态机/字段/重复请求（`views/outlets/OutletDashboard.vue`）
1) 状态映射对齐后端（`OrderService.java:98`：1 待接单 / 2 制作中 / 3 已完成 / 4 已拒绝 / 5 已取消 / 6 已换网点）：
```ts
const STATUS_TEXT = { 1: '待接单', 2: '制作中', 3: '已完成', 4: '已拒绝', 5: '已取消', 6: '已换网点' }
// stats.completed 统计 status === 3；tag type 3 → 'success'、4 → 'danger'
```
2) `120` 行 `row.assigned_at` → `row.assignedAt`（后端 `OutletService.java:469`）。
3) `245-250` "今日新增"改为按 `createdAt` 当日过滤（或接后端统计接口），不再用 `allOrders.length`。
4) 挂载重复请求（224-227 + 344-347）：`onMounted` 只调 `loadOutlets()`，去掉末尾重复 `loadData()`；`loadData` 内部改为 `if (selectedOutlet.value)` 守卫。
5) `333/337/341` `window.location.href` → `router.push`。

### P1-3 印章管理初始化重复请求（`views/products/seals.vue:860 + 1258-1278`）
聚合加载完成后设置 `activeSceneId` 触发 watcher 覆盖数据：增加聚合标志位，聚合分支内跳过 watcher：
```ts
let aggregated = false
// 聚合加载完成后：
aggregated = true
activeSceneId.value = ...
// watch(activeSceneId) 开头：
if (aggregated) { aggregated = false; return }
```

### P1-4 场景/报纸初始化无 catch（`scenes.vue:311-316`、`newspapers.vue:1001-1005`）
```ts
// scenes.vue
onMounted(async () => {
  await Promise.allSettled([getSeals(), getSealPackages()])
  loadScenes()
})
// newspapers.vue
onMounted(async () => {
  try { await getNewspaperCategories() } catch { /* 拦截器已提示 */ }
  fetchNewspapers()
})
```

### P1-5 统一响应解包写法（系统性）
- 为"刻意双层包装"的 transaction 端点提供显式解包（避免各页面手工 `.data`）：
```ts
// api/index.ts
const unwrapV1 = (res: any) => (res && typeof res === 'object' && 'data' in res ? res.data : res)
export const getTransactionStats = async (params?: any) => unwrapV1(await request.get('/transaction/stats', { params }))
```
- 存量页面按"已归一化"契约直取：`res.list ?? []`、`res.items ?? []`、`Array.isArray(res) ? res : []`，消灭 `xxx.data ??` 写法（重点：`OrderAssign:359`、`bookkeeping:274`、`refund:109`、`detail:633`、`OutletDashboard:223/242/244` 等）。

### P1-6 交易流水时区（`transaction/index.vue:366`）
```ts
function fmtLocalDay(d: Date) {
  const pad = (n: number) => String(n).padStart(2, '0')
  return `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())}`
}
// 快捷筛选 startDate/endDate 改用 fmtLocalDay(now)/fmtLocalDay(now - k*86400000)
```

---

## 三、P2 / 质量改进方案

| 项 | 位置 | 改法 |
|---|---|---|
| ESLint 缺失 | 项目根 | 新增 `eslint.config.js`（flat config：`@vue/eslint-config-typescript` + `eslint-plugin-vue` 推荐集），`package.json` 增加 `eslint`/相关 devDeps；`npm run lint` 可用 |
| console.log 残留 | TrendChart/ModuleDonut/transaction:308/OutletList:877/outlet Login 等 38 处 | 删除调试日志；`console.error` 改为 `console.error('[模块]', e)` 或统一日志工具 |
| `any` 泛滥 / finance 未开 TS | api 层、各页面 | 为 API 参数/返回定义接口类型；`finance/index.vue` `<script setup>` 补 `lang="ts"` 并给 `overview/trendData` 等加类型 |
| 取消确认误报 | questions/reviews/users 等 `catch(e){ if(e!=='cancel') }` | 统一改为 `if (e !== 'cancel' && e !== 'close')`，或 `.then(()=>true).catch(()=>false)` |
| formatDate 重复实现 | 约 15 个文件 | 统一使用 `@/utils/format` 的 `formatDate`，并给空值/非法日期返回 `'-'`（现有 utils 已处理），删除各文件本地实现 |
| 金额单位口径 | refund-records:142/144、detail:553 | 与后端确认 `payPrice/refundFee` 单位后统一；建议后端统一"分"，前端集中 `/100` 工具（`formatMoney`） |
| 按钮防重复提交 | points:32、users:31-33、menu-roles、admins 等 | 提交/删除按钮绑 `:loading="xxx"`；`handleLogin` 入口加 `if (loading.value) return`（修复 outlet Login 回车双触发） |
| 筛选不重置页码 | after-sales:41、refund-records:7/14、newspapers:54-84、OrderAssign、OutletList:68-71 等 | 所有 `@change` 筛选动作统一先 `page = 1` 再加载 |
| 死代码清理 | fetchAdminInfo、refund/index.vue、detail.vue 只读分支、seals:795 mergedItems、newspaper-templates:958-971 等 | 按 P0-5/P0-6/P0-8 处理原则删除或接入 |
| 未提交改动 | 工作区 39 个文件 +1900/-971 | 尽快 `git add -A && git commit`（或开工作分支），防止丢失 |
| PM2 配置 | `ecosystem.admin.config.cjs` | 修正 `cwd` 为实际部署路径、日志路径；生产建议改为 `vite preview`/静态托管而非 dev server |

---

## 四、系统性改造建议（可选，建议单独立项）

1. **契约对齐机制**：以后端 DTO 为准建立 `src/api/types.ts`（或接入 openapi 生成），页面/API 共用类型，杜绝 camelCase/snake_case 手工漂移（本次已核实 4 处：`settlementCycle`、`total_orders`、`assignedAt`、`enable_sections`）。
2. **金额单位常量**：`src/constants/units.ts` 定义 `FEN = 100`，金额展示统一走 `formatMoney(分)` / `formatYuan(元)` 工具。
3. **响应解包统一**：`request`/`v2Request` 之后禁止页面再出现 `.data`；双层包装端点全部在 api 层解完。
4. **CI 门禁**：`npm run lint && npx vue-tsc --noEmit && npm run build` 接入提交检查。

---

## 五、实施顺序与回归验证清单

**建议实施顺序**：P0-1 → P0-3 → P0-9（一行改动见效）→ P0-2 → P0-4 → P0-5 → P0-6 → P0-7 → P0-8 → P0-10（需后端配合）→ P1 → P2。

**每步验证**：`npx vue-tsc --noEmit` 零错误；涉及接口的改动用浏览器/后端日志确认请求参数与响应。

**回归清单（按模块）**：
- [ ] 订单分配：`/outlets/assign` 与三个历史订单列表的分配弹窗均有网点可选，推荐网点置顶
- [ ] 财务总览：趋势图首次进入可渲染；导出可下载
- [ ] 交易流水：日期筛选（尤其凌晨时段）、订单跳转、导出
- [ ] 网点管理：新增/编辑结算周期回显；总览页 KPI 有真实数据
- [ ] 网点看板/工作台：状态文案正确（3=已完成）、今日新增口径、无重复请求
- [ ] 内容管理：富文本保存后无脚本执行；上传正常
- [ ] 登录：密码错误提示"用户名或密码错误"而非"登录已过期"；无未处理 rejection
- [ ] 全局：刷新页面（含损坏 localStorage 场景）不白屏；`npm run build` 通过

---

*本方案与《前端审计报告_2026-08-26_v2.md》配套使用；实施时如遇行号漂移，以文件当前内容为准。*
