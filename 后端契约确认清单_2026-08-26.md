# 后端契约确认清单（两个后端配合项）

> 日期：2026-08-26 ｜ 来源：待办提醒 + 之前清单（`E:\代码\taro\小程序前端问题修复方案_2026-08-22.md`「需后端联调项」）
> 核对基准：`E:\代码\java\seal-backend`（Java Spring Boot）源码逐文件实读 + 前端 `E:\代码\taro\src` 实读

---

## 一、小程序码接口 `GET /api/share/qrcode` —— ✅ 已实现（待同步部署）

### 前端现状（已就绪，勿改）
- `E:\代码\taro\src\utils\api.js:447-448`：`getShareQrCode: () => request({ url: '/api/share/qrcode' })`
  - 注释约定：**返回 `{ url }` 或图片 URL 字符串**
- `E:\代码\taro\src\pages\home\index.js:374-386`（onOpenPoster）：
  - `res.url || res.imageUrl`，`typeof res === 'string'` 兜底
- `home/index.js:435-452`：`Taro.getImageInfo({ src: qrUrl })` 直接下载 → **必须是完整绝对地址**（相对 `/uploads/...` 无法被 getImageInfo 解析）

### 后端实现（新增，已编译验证）
| 文件 | 说明 |
|---|---|
| `controller\ShareController.java`（新增） | `GET /api/share/qrcode`，公开接口（无 `@UserAuth`，未登录也可生成）；可选参数 `scene`（默认 `home`，>32 字符截断）、`page`（默认 `pages/home/index`）；按 `sha1(scene|page)` 落盘 `uploads/share/{hash}.jpg` 并复用（避免重复调微信接口）；返回 `{ url: <绝对地址> }`（由请求 Host 推导，`forward-headers-strategy: framework` 下反代后自动取公共 HTTPS 域名） |
| `service\WechatApiClient.java`（改） | 新增 `getUnlimitedQrCode(scene, page)`：`POST wxa/getwxacodeunlimit`，40001（token 失效）自动刷新重试一次；未配置 appId/secret 抛 400「未配置微信小程序凭证」；JSON 错误与图片字节自动判别 |
| `service\UploadService.java`（改） | 新增 `saveBytes(...)`（字节落盘，防路径穿越）+ `urlIfExists(...)`（文件存在直接复用） |

- **响应契约**：成功 `{ code: 0, data: { url } }`（前端 request 自动解包出 `{url}`）；失败 400/500 → 前端 `.catch(() => {})` 静默，海报保持占位文案「后端接口就绪后自动显示」
- **编译验证**：`mvn compile` **BUILD SUCCESS**（259 个源文件，含 3 个改动文件）

### 待办（部署侧）
1. 运行 `E:\代码\admin\.tools\apply-qrcode-patch.ps1` 把 3 个文件同步到 `E:\代码\java\seal-backend`（与 `.tools\backend-runtime`）
2. 配置小程序凭证：`seal.wechat.app-id` / `seal.wechat.app-secret`（`application.yml` 或运行时配置表，`ConfigService` 优先读运行时表）
3. 重新构建并重启后端（当前 8080 未监听）
4. 备注：`env_version` 默认 `release`（生产海报正确）；如需体验版联调可后续给接口加 `envVersion` 参数

---

## 二、P1 联调项（P1-6/9/12/13/14/16/23）逐项核对

| # | 契约项 | 前端现状 | 后端现状 | 结论 |
|---|---|---|---|---|
| P1-6 | 实名认证：微信核验 + 身份证上传 | `api.js:476-483`：`POST /api/user/realname-verify/wechat`（body `{encryptedData, iv}`，期望 `{status:1, name, idNumberMask, verifyTime}`）、`POST /api/user/realname-verify`（body `{name, idNumber, idCardFront, idCardBack}`）、`GET /api/user/realname-verify`；realname 页上传到 `POST /api/upload/id-card` | **后端完全缺失**：`UserController` 仅 `/api/users/**`，无 realname-verify 路由；`UploadController` 无 `/id-card` 路由（仅 /image、/user-image、/user-material、/images、/material、/seal、/outlet-image） | ❌ **需后端新增**（对齐 NestJS user.controller 实名部分：上传 + 提交 + 微信核验三个端点） |
| P1-9a | `wxLogin` 手机号 code | `api.js:454-468`：`POST /api/auth/wx-login` body `{code, phoneCode?}` | `AuthController.java:34-36` 只取 `dto.get("code")` 传给 `authService.wxLogin(code)`；`AuthService.java:77-111` 不处理 phoneCode，用户手机号永不落库 | ❌ **需后端接收 phoneCode**：`wechatApiClient.getPhoneNumber(phoneCode)` → 更新 `user.phone`（已有 `WechatApiClient.getPhoneNumber` 可复用） |
| P1-9b | 代理记账计价参数命名 | `bookkeeping-tab/index.js:126-127`：`getBookkeepingPrice({ taxpayer_type, cycle, invoice, social, fund })`（snake_case） | `BookkeepingController.java:37-44`：`@RequestParam taxpayer_type/cycle/invoice/social/fund` ✓；`BookkeepingService` 返回 `{price}` ✓ | ✅ **已对齐** |
| P1-12 | V2 登报订单 DTO 发票字段 | `newspaper/form/index.js:697`：v2dto 传 `invoice: that.data.invoice \|\| null` | `OrderV2CreateService.createNewspaperOrder`（:144-208）不读取 invoice；`NewspaperOrderDetail` 实体无 invoice 字段；`OrderV2` 仅 `invoiceStatus`（且恒 `not_required`） | ❌ **需后端支持**：创建接口接收 invoice 并落库（V1 `SealOrder.invoiceJson` 可参考）；确认 V2 订单详情/结算是否透出 |
| P1-13 | V2 登报订单 DTO 期数字段 | `newspaper/form/index.js:691`：v2dto 传 `issueCount` | 同上：创建接口不读 issueCount；`NewspaperOrderDetail` 无 `issueCount` 字段（有 `copies` 但无期数）→ **下单后期数丢失** | ❌ **需后端支持**：实体加 `issueCount` 列 + 创建/详情透出 |
| P1-14 | `/api/v2/user/orders/:orderNo/pay` 响应结构 | `newspaper/form/index.js:742-751`：**`if (pay.params && pay.params.devMode)`**（devMode 在 params **内**）；`paymentNo` 取 `pay.paymentNo \|\| pay.params.paymentNo` | `OrderV2PayService.java:110`：**`r.put("devMode", true)` 在响应顶层**（params 内为空字符串）；`paymentNo` 顶层 ✓ | ⚠️ **不一致**：后端 devMode 放顶层，前端查 params.devMode → 开发模式分支永不命中（`params.package` 为空串为 falsy → 直接「支付失败」）。**二选一**：后端把 devMode 放进 params，或前端改回 `pay.devMode`（当前按前端契约为准，后端改） |
| P1-16 | 报纸计价参数命名 | `newspaper/form/index.js:539-544`：`getNewspaperPrice({ newspaper_id, content_length, issue_count, copy_count, section_id })`（**全 snake_case**） | `NewspaperController.java:104-112`：`@RequestParam newspaper_id / contentLength / issueCount / copyCount / section_id`（**contentLength/issueCount/copyCount 为 camelCase**）→ 前端传的 `content_length` 等解析为 null，计价取默认值失真 | ⚠️ **不一致**：后端改参名为 `content_length / issue_count / copy_count`（与前端契约对齐） |
| P1-23 | 供应商订阅消息模板 ID | `taro/src/utils/config.js:14`：`WECHAT_SUBSCRIBE_TEMPLATE_ID = ''`（留空时前端开关提示「通知模板暂未配置」） | `application.yml:65`：`subscribe-template-id: ""`；`WechatApiClient.sendNewOrderSubscribeMessage` 模板 ID 为空时静默跳过；`SupplierV2Controller` 的 `PUT /api/v2/supplier/me/subscribe` + `GET /me/subscribe-status` 已实现 ✓ | ⏸ **上线阻断**：需在微信公众平台「功能→订阅消息」申请模板（字段：thing 提醒语 / character_string 订单号 / thing 业务类型 / thing 网点名称），模板 ID 同时填入前端 config.js 与后端配置（yml 或运行时配置表），两端必须一致 |

---

## 三、汇总：后端需处理事项

1. **立即**：同步 qrcode 补丁（apply-qrcode-patch.ps1）+ 配置 appId/appSecret + 重启
2. **P1-14**：`/pay` 响应 devMode 移入 `params`（1 行）
3. **P1-16**：`/newspapers/price` 参名改 snake_case（3 个 @RequestParam 改名）
4. **P1-9a**：`wxLogin` 接收 phoneCode 并绑定手机号（AuthController + AuthService）
5. **P1-12/13**：V2 登报订单 DTO 支持 `invoice`/`issueCount`（实体加字段 + 创建/详情透出）
6. **P1-6**：实名认证三个端点（上传身份证、提交核验、微信核验、查询状态）
7. **P1-23**：申请订阅模板 ID 并双端配置（前后端配合）
