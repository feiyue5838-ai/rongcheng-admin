# P2 质量改进补丁包 —— 应用说明

> 配套：《前端审计修复方案_2026-08-26.md》P2 部分 ｜ 生成日期：2026-08-26
> 交付方式：完整修复文件副本（`P2修复3\src\...`），16 个文件全部通过 TypeScript 语法/类型转换校验（16/16 OK）。
> 叠加关系：本包文件为"当前最优版本"——**凡在 P0/P1 改过的文件，本包副本已同时包含 P0/P1/P2 全部改动**，直接覆盖即可，无需再按 P0→P1→P2 顺序叠加。

---

## 一、本包文件（16 个，相对仓库根）

| # | 文件 | 修复内容 | 叠加自 |
|---|---|---|---|
| 1 | `src/views/outlet/Workspace.vue`-（不含） | — | — |
| 1 | `src/views/transaction/index.vue` | 清理 console.log | P0-7 + P1-6 + P2 |
| 2 | `src/views/outlets/OutletList.vue` | 页码语义/筛选复位/close | P0-9 + P2 |
| 3 | `src/views/products/seals.vue` | 格式串行（无 formatDate，跳过） | P1-3 + P2 |
| 4 | `src/views/outlets/OrderAssign.vue` | 筛选/搜索重置页码、删未用导入 | P0-1 + P2 |
| 5 | `src/views/after-sales/after-sales-orders.vue` | 模块筛选重置页码 | P0-5 + P2 |
| 6 | `src/stores/auth.ts` | 删除死代码 fetchAdminInfo | P0-3 + P2 |
| 7 | `src/views/products/newspapers.vue` | 筛选变化回第一页（watch） | P1-4 + P2 |
| 8 | `src/views/points/index.vue` | 保存防重复提交（loading） | P1-5 + P2 |
| 9 | `src/views/after-sales/refund-records.vue` | 筛选/查询重置页码 | P2 |
| 10 | `src/views/questions/index.vue` | 取消确认补 'close'；formatDate 空值 | P2 |
| 11 | `src/views/reviews/index.vue` | 取消确认补 'close' | P2 |
| 12 | `src/views/users/index.vue` | 取消确认补 'close' | P2 |
| 13 | `src/views/system/admins.vue` | formatDate 空值保护 | P2 |
| 14 | `src/components/ModuleDonut.vue` | 删除 console.log 残留 | P2 |
| 15 | `src/views/login/index.vue` | 登录防重复提交（loading 守卫） | P2 |
| 16 | `src/views/outlet/Login.vue` | 回车双触发防重 | P2 |

**另有新增文件**：仓库根目录 `eslint.config.js`（ESLint flat config，见第三节）。

---

## 二、应用步骤（推荐：复制替换）

在仓库根目录（`E:\代码\admin`）用 PowerShell 执行：

```powershell
$files = @(
  'src/views/transaction/index.vue','src/views/outlets/OutletList.vue',
  'src/views/products/seals.vue','src/views/outlets/OrderAssign.vue',
  'src/views/after-sales/after-sales-orders.vue','src/stores/auth.ts',
  'src/views/products/newspapers.vue','src/views/points/index.vue',
  'src/views/after-sales/refund-records.vue','src/views/questions/index.vue',
  'src/views/reviews/index.vue','src/views/users/index.vue',
  'src/views/system/admins.vue','src/components/ModuleDonut.vue',
  'src/views/login/index.vue','src/views/outlet/Login.vue'
)
foreach ($f in $files) { Copy-Item -Force "P2修复3\$f" $f }
Write-Host 'P2 fixes applied'
```

> 若 IDE 正打开这些文件，请先关闭或允许外部修改；应用后选择重新加载。

### 备选：git apply

`P2修复3\apply-me.patch`（34KB，16 个文件）。CRLF 差异导致 `patch does not apply` 时先转 CRLF：

```powershell
$p = Get-Content -Raw 'P2修复3\apply-me.patch'
$p = $p -replace "`r`n", "`n" -replace "`n", "`r`n"
Set-Content -NoNewline -Encoding utf8 'P2修复3\apply-me.crlf.patch' $p
git apply 'P2修复3\apply-me.crlf.patch'
```

---

## 三、各 P2 项说明

### 1. ESLint 工具链（新增 eslint.config.js）
- 仓库根已新增 `eslint.config.js`（flat config：`@eslint/js` + `eslint-plugin-vue` + `@vue/eslint-config-typescript`）。
- 需安装依赖后才能运行：`npm i -D eslint eslint-plugin-vue @vue/eslint-config-typescript`，随后 `npm run lint`（package.json 已有 lint 脚本）。
- 规则对存量代码宽松（`no-explicit-any` 关闭、`no-console` 仅 warn 且放行 warn/error），后续可逐步收紧。
- ⚠️ 说明：package.json 在审计环境被锁无法修改，依赖需你手动安装。

### 2. console.log 调试残留
- `ModuleDonut.vue`、`transaction/index.vue:308`（打印完整选项数组）已删除；`TrendChart.vue` 已在 P0 清理。
- 各文件 catch 中的 `console.error`（合法错误日志）保留。

### 3. ElMessageBox 取消误报（'close'）
- `questions`（3 处）、`reviews`（2 处）、`users`（2 处）、`OutletList`（1 处）：`e !== 'cancel'` 补全为 `e !== 'cancel' && e !== 'close'`，ESC/右上角关闭不再误弹"操作失败"。

### 4. formatDate 空值保护
- `questions/index.vue`、`admins.vue`：`d ? dayjs(d).format(...) : '-'`，不再对 null 显示 "Invalid Date" 或当前时间。
- 注：`seals.vue` 经复核当前版本已无 formatDate 函数（模板不依赖），无需改动。
- 全仓统一 `@/utils/format` 的 formatDate 属更大重构（涉及 ~15 文件、展示格式差异），本次未做，列为后续项。

### 5. 筛选/搜索重置页码
- `OrderAssign`：搜索/切 Tab/模块/重置统一走 `search()`（先 `page=1`）；删除未使用的 `getOutletsAPI` 导入。
- `OutletList`：`loadData()` 不再强制回第 1 页（编辑/删除后保持当前页）；筛选变更走 `onFilterChange()`（显式回第 1 页）。
- `refund-records`、`after-sales-orders`：筛选/查询先重置页码。
- `newspapers`：新增 watch，任一筛选变化回第 1 页。

### 6. 防重复提交
- `login/index.vue`、`outlet/Login.vue`：`handleLogin` 入口加 `if (loading.value) return`（同时修复网点登录回车双触发）。
- `points/index.vue`：保存按钮加 `:loading="saving"` + `save()` 防重。

### 7. 死代码清理
- `auth.ts`：删除从未被调用的 `fetchAdminInfo`（含 `getAdminInfo` 导入与导出）。

### 8. 金额单位 / finance lang="ts" / any 收敛（未做，说明）
- **金额单位**（refund-records:142/144、detail:553）：需与后端确认 `payPrice/refundFee` 单位后再统一，避免改错放大金额，本次不动。
- **finance/index.vue 补 `lang="ts"`**：整文件为无类型 JS，开启严格类型检查会产生大量隐式 any 报错，需配套补类型，列为后续重构。
- **`any` 全仓收敛**：建议新增 `src/api/types.ts` 契约类型后逐页替换，属长期工作。

---

## 四、应用后验证

```bash
npx vue-tsc --noEmit     # 0 错误
npm run build
```

手工回归：
- [ ] 各确认弹窗按 ESC/右上角关闭不再弹错误提示
- [ ] 列表翻到第 5 页后切换筛选，自动回到第 1 页（OrderAssign/OutletList/refund-records/after-sales/newspapers）
- [ ] 编辑/删除网点后停留在当前页（不再跳回第 1 页）
- [ ] 登录按钮连点/回车不重复提交
- [ ] 积分奖品保存按钮有 loading、不可连点
- [ ] 问答/管理员列表日期为空时显示 '-' 而非 Invalid Date
- [ ] 交易流水导出后控制台无调试日志

---

## 五、残留清理

- 本次生成的中间目录可删除：`P2修复\`（首次损坏写入，勿用）、`P2修复2\`（校验过程目录）；**最终使用 `P2修复3\`**。
- 根目录校验脚本 `.audit-check-p2.mjs`、`.audit-check-p1.mjs`、`.audit-check-fixed.mjs` 可删除。
- 应用后尽快 `git add -A && git commit`（工作区仍有大量未提交改动）。
