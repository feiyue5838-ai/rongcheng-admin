# 蓉城企服管理后台（admin）前端代码审计报告

- **审计日期**：2026-08-26
- **审计范围**：`E:\代码\admin`（Vue 3 + TypeScript + Vite + Element Plus + Pinia + vue-router 4 + axios + echarts）
- **审计对象**：`src/` 全部源码（约 55 个 .vue/.ts 文件，约 800KB），含后端契约交叉核对（`E:\代码\java\seal-backend` 源码直读）
- **审计方法**：静态逐文件审读 + 工具验证（vue-tsc 类型检查）+ 后端源码契约核对 + 全局模式扫描（console / v-html / localStorage / 定时器 / 解包模式等）

---

## 一、执行摘要

| 级别 | 数量 | 说明 |
|---|---|---|
| 🔴 高（P0/P1） | 10 类 16 处 | 功能不可用、数据错误、安全风险 |
| 🟠 中 | 约 28 处 | 边界缺陷、明显问题、功能缺失 |
| 🟡 低 | 约 45 处 | 代码质量、规范、健壮性细节 |

**Top 7 最需优先处理的问题：**

1. **订单分配页网点列表恒为空** —— `views/outlets/OrderAssign.vue:359` 与 `views/orders/bookkeeping.vue:274` 对 `/dispatch/outlets/available`（后端返回**数组**，已核实 `DispatchService.getAvailableOutlets` 返回 `List`）做 `(res as any).data ?? []` 二次解包，`res.data` 恒为 undefined → 分配弹窗永远没有可选网点，**派单功能不可用**。
2. **财务总览趋势图首次进入永不渲染** —— `components/TrendChart.vue` 图表容器由 `v-else` 控制，仅在数据非空时渲染；`onMounted` 时数据为空 → `chartRef` 为 null → 初始化中止；数据到达后 `watch` 只调 `renderChart()`，而 `chartInstance` 仍为 null 直接 return。已核实 `finance/index.vue:55-60` 以空数组挂载该组件。
3. **刷新白屏风险** —— `src/stores/auth.ts:10-12` 对 `localStorage.admin_info` 的 `JSON.parse` 无 try/catch，`main.ts` 在挂载前创建 store，一旦 localStorage 被损坏/篡改即整站白屏（网点端 `stores/outlet.ts` 已对 `'undefined'` 字符串做防护，管理端未做）。
4. **内容管理富文本存储型 XSS** —— `views/content/index.vue:733-743` `normalizeRichText` 只要 HTML 含 `<img|video|audio|iframe|table>` 即原样入库，未做任何净化（事件属性、`<script>` 可透传）；工具栏 `excludeKeys: []` 未禁源码编辑，管理员可粘贴恶意 HTML → 小程序/客户端渲染时存在存储型 XSS。
5. **历史订单管理功能大面积不可达（死代码）** —— `seal.vue / newspaper.vue / bookkeeping.vue / after-sales-orders.vue` 的"发货/分配网点/退款/售后"对话框在模板中**没有任何触发按钮**（操作列仅有"详情"），对应管理动作无法执行；`bookkeeping.vue` 还**缺少分页组件**，历史记账订单只能看到第一页。
6. **网点管理结算周期字段名与后端不符（功能失效）** —— `views/outlets/OutletList.vue:808-813` 提交 `settlement_cycle/settlement_weekly_start_day`（snake_case），后端 `OutletService`（230/302 行）只读 `settlementCycle/settlementWeeklyStartDay`（camelCase）→ **结算周期设置被静默丢弃，永不生效**。
7. **全网点总览页订单统计字段名与后端不符且后端为占位值** —— `views/outlets/OutletOverview.vue` 全用 `totalOrders`（camelCase），后端 `OutletService.getOverview`（503/518/540 行）返回 `total_orders`（snake_case，且当前**硬编码占位 0**）→ 累计订单、四个 KPI、柱状图、表格"累计"列全部失效。

---

## 二、工具验证结果

| 验证项 | 结果 |
|---|---|
| `vue-tsc --noEmit`（严格模式） | ✅ 0 错误（代码可类型通过，但大量 `any` 使类型约束形同虚设） |
| `vite build` 生产构建 | ⚠️ 未能在沙箱内完成（esbuild 子进程需命名管道，被安全沙箱拦截；非项目缺陷，无法在本次环境复验） |
| ESLint | ❌ `npm run lint` **不可用**：项目未安装 eslint、无任何 eslint 配置文件（`.eslintrc*` / `eslint.config.*` 均不存在），运行时会尝试联网下载 eslint 并失败。建议补充 ESLint + 配置。 |
| 后端契约核对 | 交叉读取 `seal-backend` 的 `ResponseWrapAdvice`、`DispatchController/Service`、`TransactionController`、`RefundService`、`NewspaperService`、`SealService`、`OutletService`、`OrderService` 等，用于甄别真缺陷与误报（见第八节） |

---

## 三、高危问题（P0/P1）明细

### P0-1 订单分配网点列表恒为空（派单不可用）
- 位置：`views/outlets/OrderAssign.vue:359`、`views/orders/bookkeeping.vue:274`（同因 `views/orders/newspaper.vue:346` 有 `?? res` 兜底，幸免）
- 根因：`request` 拦截器已解 `{code:0,data}` 单层包装；`/dispatch/outlets/available` 后端返回 `List`（已核实 `DispatchService.java:193`），解包后 `res` 即为数组。前端再写 `(res as any).data ?? []` → `res.data` 为 undefined → 恒为空数组。
- 影响：`/outlets/assign` 订单分配页与记账订单分配弹窗永远无可选网点，**功能完全不可用**。
- 建议：统一改为 `Array.isArray(res) ? res : (res?.data ?? [])`；同时排查全仓所有 `xxx.data ??` 二次解包写法（见第五节系统性说明）。

### P0-2 TrendChart 趋势图首次加载永不渲染
- 位置：`components/TrendChart.vue:12-15 / 78-118 / 190-192`
- 根因：图表容器 `v-else` 只在数据非空时渲染；组件 `onMounted` 时父组件数据为空 → `chartRef` null → 初始化中止；此后数据到达，`watch(props.data)` 仅调 `renderChart()`，因 `chartInstance` 为 null 直接 return，**没有任何代码再次调用 `initChart()`**。
- 影响：财务总览"近 N 天交易趋势"图首次进入页面必现为空（已核实 `finance/index.vue:55-60` 以空 `trendData` 挂载）。
- 建议：`watch` 中数据由空转非空时调用 `initChart()`，或去掉 `v-else` 让容器常驻（参照 `ModuleDonut.vue` 的处理）。

### P0-3 登录态 localStorage 损坏导致整站白屏
- 位置：`src/stores/auth.ts:10-12`（配合 `src/main.ts:24-35` bootstrap 在挂载前创建 store）
- 根因：`JSON.parse(localStorage.getItem('admin_info') || 'null')` 无 try/catch。历史上有 `'undefined'` 字符串残留（网点端 store 已为此加防护，管理端未加）。
- 建议：`try { ... } catch { null }` 包裹解析；顺带清理 `fetchAdminInfo`（`auth.ts:45-49`，定义后从未被调用，属死代码）。

### P0-4 内容管理富文本存储型 XSS
- 位置：`views/content/index.vue:733-743`（`normalizeRichText`）、`363`（`excludeKeys: []` 未禁用源码编辑）
- 根因：HTML 一旦含媒体/表格标签即整段原样入库，无白名单净化；`<img onerror=...>`、`<script>`、iframe 可透传，后续在小程序/客户端渲染时触发。
- 建议：用 DOMPurify 等白名单方案净化后再保存，或由后端统一过滤；至少禁掉源码编辑模式与 iframe。

### P0-5 历史订单页管理操作无入口 + 记账页缺分页
- 位置：
  - `views/orders/seal.vue:92-96`（操作列仅"详情"，112-193 发货/分配对话框不可达）
  - `views/orders/newspaper.vue:88`（同）
  - `views/orders/bookkeeping.vue:107`（同，且全文件无 `el-pagination`，`total` 在 345 行已取但从未渲染 → 只能看第一页 20 条）
  - `views/after-sales/after-sales-orders.vue:89-93`（"只读"文案，109-135 退款/售后对话框不可达）
- 建议：为操作列补齐按钮并接好对话框，或明确降级为只读并删除死代码；记账页补分页组件。

### P0-6 订单详情页"发货"实现为双重缺陷（当前被硬编码只读掩盖）
- 位置：`views/orders/detail.vue:324`（`legacyReadOnly = true` 硬编码，使 157-199 管理卡片与全部对话框不可达）、`362-396`（`confirmDeliver`）
- 根因（一旦放开只读即触发）：
  1. `updateOrder(...)` 提交 camelCase 字段 `expressCompany/expressNo`，而后端 `adminUpdateOrder` 白名单为 snake_case（`express_company/express_no`），**快递信息会被静默丢弃**；
  2. 随后又用原生 `window.fetch` 调 `/api/orders/:id/deliver-admin`，**不带 Authorization 头**（管理端接口受 AdminJwtAuthGuard 保护，必 401）、不检查响应状态，且与第 1 步构成**重复发货调用**。
- 建议：统一走 axios 封装的管理端发货接口，字段用 snake_case，删除原生 fetch；若页面确为只读，删除整套死逻辑。

### P0-7 交易流水页"导出"未实现 + "查看订单详情"跳转失效
- 位置：`views/transaction/index.vue:424-440`（`onExport` 仅提示"导出成功"，无下载触发，注释自认"Blob 后续可触发下载"）、`452`（`window.open('/#/orders/detail/${orderId}')`）
- 根因：导出未创建 Blob 下载链接；且后端 `/transaction/export` 实际返回 JSON（`{code:0,data:{data:[...],filename}}`）而非文件流，需要后端配合改二进制导出。跳转用 hash 风格 URL 而路由是 `createWebHistory`，且路径段数也不匹配 `orders/:id` 路由 → 落到兜底重定向回工作台。
- 建议：参照 `exportOrders`（`api/index.ts:95-96`，blob + 60s 超时）实现下载；跳转改为 `/orders/${orderId}` 或 `router.push`。

### P0-8 退款管理（V1）页面为无路由死代码 + 解包错误
- 位置：`views/refund/index.vue`（整页）—— 路由 `router/index.ts:88/93` 已将 `/refund`、`/finance/refund` 重定向到 `/v2/refunds`，本页无任何路由可达；
- `refund/index.vue:109-110` `res.data?.items` 与解包契约冲突（后端 `RefundService.list` 返回 `{items,total}` 经单层解包后 `res` 即 `{items,total}`，`res.data` 为 undefined → 列表恒空）。若保留应改为 `res.items ?? res.data?.items ?? []` 并修正 `viewOrder` 的跳转参数（166-168 用数字主键当 keyword，无法定位订单）。
- 建议：删除或接入路由并修复；当前为被 V2 页取代的遗留文件。

### P0-9 网点结算周期字段名与后端不符（功能失效）
- 位置：`views/outlets/OutletList.vue:808-813`
- 根因：payload 用 `settlement_cycle`/`settlement_weekly_start_day`（snake_case），后端 `OutletService.create`（230-231 行）与 `update`（302-304 行）只读 `settlementCycle`/`settlementWeeklyStartDay`（camelCase）→ 字段被静默丢弃。
- 影响：新增/编辑网点时结算周期与每周起算日**永不生效**（默认周结逻辑可能被静默使用）。
- 建议：提交改为 `settlementCycle`/`settlementWeeklyStartDay`；同时处理 `el-select clearable` 清空后 `''` 与 `null` 的边界（`OutletList.vue:222-227`）。

### P0-10 全网点总览页订单统计字段名与后端不符（且后端为占位值）
- 位置：`views/outlets/OutletOverview.vue:40/73/130/182/225-248/305`
- 根因：前端读 `summary.totalOrders / regions[].totalOrders / outlets[].totalOrders`（camelCase），后端 `OutletService.getOverview`（503/518/540 行）返回 `total_orders`（snake_case）→ 字段对不上；且后端当前 `total_orders` 等统计**硬编码占位 0**（未做真实聚合）。
- 影响："累计订单"卡为空、接单率/待接占比/完成率/平均单店四个 KPI 恒 0.0%、`kpi-foot` 显示"已接 NaN"、柱状图订单量系列为空、两个表格"累计"列空白——页面核心数据整体不可用（即使前端改对字段名，后端也需补聚合实现）。
- 建议：前端字段名与后端对齐（或后端统一 camelCase，注意 `/outlets` 列表接口是 camelCase `totalOrders`，后端自身两套命名）；后端补全 `getOverview` 聚合逻辑。

---

## 四、中等问题

### 4.1 网点工作台（views/outlet/Workspace.vue）
- [中] `461-468` `getAuthorizedOrders`：网点 `businessTypes` 为空（未配置业务类型）时 `codes.length === 0` 直接 `return []` → **整个工作台订单列表与统计置空且无提示**。
- [中] `473/483/494` 列表硬编码 `{page:1, pageSize:100}` 且仅前端过滤、无翻页 → 超 100 条订单不可见，统计卡片失真。
- [中] `407-409` `previewMaterial` 无条件 `window.location.origin + url`，材料 URL 为绝对地址时拼出坏链接（`412-413` 的 `getMaterialUrl` 已有正确的绝对地址判断，应复用）。
- [低] `637-646` 轮询恒传 `showToast=false` 且 `lastUnread>0` 逻辑使"新通知"toast 分支永不触发（死逻辑）；轮询只刷通知不刷订单（新派订单需手动刷新）。

### 4.2 印章管理（views/products/seals.vue）
- [中] `860 + 1258-1278` 初始化先聚合加载全部场景（约 16 个请求），随后设置 `activeSceneId` 触发 `watch` 重新拉取当前场景并**覆盖** `seals/packages` → 聚合请求白费 + 其他场景 Tab 角标计数归零（`sceneItemCount` 798 行），每次切 Tab 复现。
- [低] `1219-1225` `BUSINESS_SCENE_NAMES` 名称硬编码白名单与"场景全部来自接口"注释矛盾；无匹配时回退返回全部分类（含个人/电子场景），存在分类串页风险。
- [低] 多处 `ElMessageBox.confirm` 无 try/catch（717-722/1046-1051/1174-1179），取消即产生未处理 rejection；`toggleStatus`（1181-1184）失败不回滚开关状态。

### 4.3 报纸管理（views/products/newspapers.vue / newspaper-templates.vue）
- [中] `newspapers.vue:1001-1005` `onMounted` 中 `await getNewspaperCategories()` 无 try/catch：该接口失败 → `fetchNewspapers()` 不执行 → 整页空白无重试。
- [中] `newspaper-templates.vue:745-749` 新增分类把表单"描述"提交为 `icon` 字段、`color` 未提交（后端 `NewspaperCategory` 实体 icon 用于前端 `iconSvg` 渲染）——疑似字段映射错误，描述文本会被当 SVG 图标使用，子分类路径（756-763）则是正确的 `desc/color` 命名，前后不一致。
- [中] `newspaper-templates.vue:980/996` `getAdminTemplates({page:1, pageSize:500})` 硬编码 500 上限，模板超 500 时前端分页与统计失真。
- [低] `newspapers.vue:826/876` 编辑回显读 `row.enableSections`（驼峰）而保存载荷键为 `enableSections`、其余字段多下划线，命名不一致有回显恒默认风险；`129` `parseFloat(pricePerWord)` 空值显示 `¥NaN`；筛选变更不重置 `pageNum`（54-84）。

### 4.4 场景管理（views/products/scenes.vue）
- [中] `311-316` `onMounted` 中 `Promise.all([getSeals(), getSealPackages()])` 无 try/catch：任一失败则 `loadScenes()` 不执行，整页不可用。
- [低] `296-297` `savePackages` 成功后 `selectScene()` 再 `loadScenes()`，而后者内部又调 `selectScene()` → 场景详情重复请求两次；`253-265` 无 `Array.isArray` 归一化；`302-309` 删除无 catch。

### 4.5 积分兑奖（views/points/index.vue）
- [中] `31` `loadOrders` 中 `orders.value = r.list; total.value = r.pagination.total` 无判空/可选链且无 catch → 接口异常抛 TypeError、列表停留旧数据。
- [中] `32` `save()` 无 `:loading` 防重复提交、无 catch → 双击可创建重复奖品。
- [低] 切"兑换订单"Tab 不重置 `page`；发货弹窗不校验快递单号必填；`reactive<any>` 滥用。

### 4.6 交易流水（views/transaction/index.vue）
- [中] `366` 快捷筛选用 `d.toISOString().slice(0,10)`（UTC）：本地凌晨 0:00-7:59 之间"今日/昨日/近7天"起始日期错算到前一天。
- [中] `424-440` 导出未实现（见 P0-7）。
- [中] `452` 订单跳转失效（见 P0-7）。
- [低] `45` 本月退款笔数复用 `stats.month?.count`（疑复制粘贴，需核对后端字段）；`363-388` 快捷筛选不重置页码；`308` console.log 残留。

### 4.7 工作台与导航（views/dashboard/index.vue）
- [中] `139 / 183` `$router.push('/outlets/list')` —— 该路由不存在（实际为 `/outlets`），命中 404 兜底重定向回工作台，**"全部 ›"/"本月 ›" 链接点击无效**。
- [低] `555` `echarts.init` 未在卸载时 dispose（反复进出工作台内存累积）；`373/378` 死代码 computed；`523` 网点列表硬编码 100 条上限；`517` 人为延迟 300ms 关 loading。

### 4.8 权限与路由
- [中] `router/index.ts:144-156` + `constants/roles.ts:79-120`：动态路由 `/orders/:id`（订单详情）不在硬编码默认权限表中（DB 无配置的兜底模式下），`order_admin/outlet_admin` 访问详情会被 403。DB 配置存在时依赖 `/orders` 前缀规则，建议补默认配置或前缀规则。
- [中] `api/index.ts:41-64` 401 处理不区分"登录失败"与"会话过期"：登录接口（`/auth/admin/login`、`/auth/Outlet/login`）返回 401 时（如密码错误）会弹"登录已过期"并 `window.location.href` 硬跳转；且 `login/index.vue:60-70` 无 catch，产生未处理 rejection。

### 4.9 网点端登录
- [中] `views/outlet/Login.vue:9/28` 密码框 `@keyup.enter` 与 form `@submit.prevent` 双入口，回车会触发两次 `handleLogin`（loading 在异步 validate 之后才置位），可能重复提交。

### 4.10 派单规则（views/system/dispatch-rules.vue）
- [中] `535` `drViewOrder` 跳转 `/orders/seal?id=${row.orderId}`：seal.vue 不读取 `route.query.id`，且 `row.orderId` 字段名与表格 snake_case 风格（`assigned_at` 等）存疑 → 跳转无效。
- [低] `412` `row.outlet.name` 未判空（缺 outlet 对象时误报"保存失败"）；`18/39/44` 开关 `@change` 即保存无防抖；`484-485` 网点下拉 100 条上限。

### 4.11 网点管理（views/outlets/ 其余页面）
- **OutletList.vue**
  - [中] `23-40 / 621-631` 统计卡片（营业中/非营业/累计订单）由当前页 `tableData`（最多 100 条）计算，翻页/筛选后失真；且后端 `findAll` 默认 `status in (0,1)`，"非营业"统计不到 2/3/4/5 状态网点。
  - [中] `297-308` 业务授权弹窗硬编码三个业务类型 UUID（`bf454c50-…` 等），种子数据变更则授权静默失败；建议从接口动态拉取。
  - [低] `30` "非营业"卡片只累计 status 0/2/4/5，遗漏"审核中"(3)，卡片合计 ≠ 总数；`822/853/860` 操作后 `loadData()` 无参强制回第 1 页；`68-71` 筛选 `@change` 不重置页码；`85` 业务类型筛选值 `accounting` 与全项目 `bookkeeping` 不一致（需核对后端 `business_types.code`）。
- **OutletDashboard.vue**
  - [中] `120` 分配时间列读 `row.assigned_at`，后端返回 `assignedAt`（`OutletService.java:469`）→ 该列恒显示 "-"。
  - [中] `209-217 / 248` 状态机映射错误：后端 assignment 状态为 1=待接单、2=制作中、3=已完成、4=已拒绝、5=已取消、6=已换网点（`OrderService.java:98`，发货后置 3），前端却把 3 显示为"已发货"、4 显示为"已完成"且 `completed` 只统计 status===4 → "已完成"统计恒为 0、状态 3 订单标签错误、**把已拒绝订单计入已完成**。
  - [中] `245-250` "今日新增"卡片实际等于接口返回的当前页订单总数（最多 100），并非今日新增。
  - [中] `224-227 + 344-347` 挂载时 `loadOutlets()` 自动选中后已 `loadData()`，`onMounted` 末尾又调一次 → 每次进入重复请求 `/outlets/{id}` 与 `/outlets/{id}/orders`。
  - [低] `228/251` catch 为空（无提示/无重试）；`333/337/341` 用 `window.location.href` 整页跳转（丢 SPA 状态）。
- **OrderAssign.vue**
  - [中] `321/330` "已分配"Tab 下 `loadStats()` 被调用两次；每次 `loadData` 还额外发一次 `pageSize:1` 计数请求（309-313 行 `resAll`），结果随后被 `loadStats` 覆盖，纯浪费。
  - [低] `10/74/81/84` 搜索/切 Tab/重置均不重置 `page`；`107/358` `row.addressJson` 在后端响应 DTO 中不存在（后端返回 `customerAddress` 对象 + `serviceRegion`），传参恒为 undefined、地区匹配评分失效；`231` `getOutletsAPI` 未使用。
  - XSS 评估：`200-211` 的 `v-html` 高亮已对 `before/hit/after` 全部 `escapeHtml` 转义（394-396），搜索词注入无 XSS 面，但后续维护须保持该转义。
- **DeliveryReceipts.vue**
  - [低] `148/193/230` 服务端分页下给列开 `sortable`，仅对当前页排序，翻页后顺序误导；`121` `:default-sort prop="createdAt"` 与列 `prop="created_at"` 不一致；`402` `.svg` 被当图片新开标签打开（恶意 svg 脚本执行面，上传源为网点端）；`381-392` 批量下载逐个 `a.click()` 易被浏览器拦截且无提示；`14` 统计条 `v-if` 条件冗余；`353-354` 死赋值；`254-257` 未用图标导入。
- **OutletOverview.vue**
  - [低] `271-273` echarts 只 init 不 dispose（内存泄漏）；`122/163/166` `topOutlets/outlets` 未判空（缺字段时模板抛错）。
  - ⚠️ 后端备注：`OutletService.getOverview()` 当前所有订单统计为占位 0，即使前端字段名修正也拿不到真实数据，需后端补全。

---

## 五、系统性问题（跨文件）

1. **响应解包写法三种并存**（最需要统一）：
   - 正确直取：`res.list`（如 `system/admins.vue:102`）；
   - 防御式：`res.data ?? res`（如 `finance/index.vue:325`）；
   - **错误二次解包**：`(res as any).data ?? []`（`OrderAssign.vue:359`、`bookkeeping.vue:274` → 恒空，见 P0-1）、`res.data?.items`（`refund/index.vue:109` → 恒空）、`res.data?.list`（`orders/detail.vue:633` 缺 `?? res.list` 兜底）。
   - ⚠️ 注意：`transaction/index.vue` 的 `resp.data` 用法与后端 transaction 端点**刻意双层包装**契约一致（`TransactionController` 每端点 `inner(...)` 再被 `ResponseWrapAdvice` 包装），**不是缺陷**（见第八节）。
   - 建议：在 `api/index.ts` 为双层包装端点提供显式解包函数或独立实例（类似 `v2Request`），页面统一直取，消灭手工 `.data`。
2. **字段命名 camelCase/snake_case 混用**（本仓最突出的系统性风险，已核实 4 处高/中危）：
   - `settlement_cycle` vs `settlementCycle`（P0-9）；
   - `total_orders` vs `totalOrders`（P0-10，后端自身两套命名）；
   - `assigned_at` vs `assignedAt`（OutletDashboard:120）；
   - `enable_sections` vs `enableSections`（newspapers.vue:826/876）；
   - `seal_ids` vs `sealIds`（经核实后端返回 camelCase `sealIds`，前端用法正确，属防御性兼容，非缺陷）。
   - 建议：以后端 DTO 为准统一命名，或引入契约类型（openapi/types）自动对齐。
3. **金额单位口径不一**：`after-sales/refund-records.vue:142` `refund.refundFee / 100`（分→元）与同文件 `144` `row.payPrice` 不除 100 并存；`orders/detail.vue:553` 也除 100。同一退款概念两个单位，需与后端核对并统一（若 `payPrice` 为分则 144 行放大 100 倍）。
4. **日期格式化重复实现**：约 15 个文件各自实现 `formatDate`（`utils/format.js` 已提供公共版本）；部分无空值保护（`seals.vue:268`、`admins.vue:93`、`questions/index.vue:199` 对 null/undefined 显示 "Invalid Date" 或 dayjs(undefined) 显示当前时间）。
5. **ElMessageBox 取消误报**：多处 `catch (e) { if (e !== 'cancel') ... }` 未排除 `'close'`（ESC/右上角关闭），会误弹错误提示（`questions`、`reviews`、`users` 等）。
6. **console.log 调试残留 38 处**：`components/TrendChart.vue` 13 处、`ModuleDonut.vue`、`transaction/index.vue:308`（打印完整选项数组）、`outlet/Login.vue:77/81`、`OutletList.vue:877` 等。
7. **`any` 滥用普遍**：api 层所有参数/返回为 `any`、多数页面 `ref<any>`/`reactive<any>`，vue-tsc 形同虚设；`finance/index.vue` 整个 `<script setup>` 未开 `lang="ts"`。
8. **异步无 catch 普遍**：大量页面 `try/finally` 或裸 `await`，失败仅靠拦截器 toast，产生未处理 rejection（`login`、`faq`、`admins`、`menu-roles`、`logs` 等）。
9. **工具链缺失**：无 ESLint 配置（lint 脚本不可用）；`auto-imports.d.ts` 被 git 追踪而 `components.d.ts` 被忽略（生成文件入库策略不一致）；`ecosystem.admin.config.cjs` 硬编码 `D:/rongcheng-admin/admin` 与 `D:/temp/logs` 路径，且以 vite dev 进程当服务（非本机可用）。

---

## 六、分模块明细（按文件）

> 各文件详细问题与行号已在上述各节覆盖，此处仅列文件级结论与遗留项。

| 模块/文件 | 结论 |
|---|---|
| `views/outlets/OrderAssign.vue` | 🔴 派单网点列表恒空（P0-1）；重复请求/页码等中低危 |
| `views/outlets/OutletList.vue` | 🔴 结算周期字段名失效（P0-9）；统计口径/硬编码 UUID 等中低危 |
| `views/outlets/OutletOverview.vue` | 🔴 订单统计字段名+后端占位值致数据全失效（P0-10） |
| `views/outlets/OutletDashboard.vue` | 🟠 状态机映射错误/字段名不符/重复请求（4.11） |
| `views/outlets/DeliveryReceipts.vue` | 🟡 服务端分页排序误导、svg 下载面、死代码（4.11） |
| `views/outlet/Workspace.vue` | 🟠 3 个功能缺陷（4.1），定时器清理正确 |
| `views/products/seals.vue` | 🟠 初始化重复请求+角标失真（4.2） |
| `views/products/newspapers.vue` | 🟠 初始化无 catch 白屏风险（4.3） |
| `views/products/newspaper-templates.vue` | 🟠 分类字段映射 + 500 上限（4.3） |
| `views/products/scenes.vue` | 🟠 初始化无 catch（4.4） |
| `views/orders/{seal,newspaper,bookkeeping}.vue` | 🔴 管理操作无入口 + 记账缺分页（P0-5） |
| `views/orders/detail.vue` | 🔴 只读硬编码掩盖发货双重缺陷（P0-6） |
| `views/after-sales/after-sales-orders.vue` | 🔴 退款/售后操作不可达（P0-5） |
| `views/after-sales/refund-records.vue` | 🟠 金额单位不一致；筛选不重置页码 |
| `views/refund/index.vue` | 🔴 无路由死代码 + 解包错误（P0-8） |
| `views/transaction/index.vue` | 🟠 导出/跳转/时区（P0-7、4.6） |
| `views/v2/*`（6 个文件） | ✅ 质量明显高于 V1；仅 100 条上限、`refundStatus` 未校验等低危 |
| `views/system/*` | 🟠 dispatch-rules 跳转失效；menu-roles 未校验角色非空；configs 死代码/文案 |
| `views/content/index.vue` | 🔴 富文本 XSS（P0-4）；上传绕 401；整体 CRUD 完整 |
| `views/dashboard/index.vue` | 🟠 死链 /outlets/list（4.7）；echarts 未 dispose |
| `views/finance/index.vue` | 🟠 弱类型；个别展示细节；逻辑闭环良好 |
| `views/faq/questions/reviews/users/points` | 🟡 取消误报、无 catch、points 防重缺失（4.5） |
| `components/TrendChart.vue` | 🔴 首载不渲染（P0-2）+ 13 处 console 残留 |
| `components/ModuleDonut.vue` | 🟡 1 处 console 残留；dispose 正确 |
| `src/stores/auth.ts` | 🔴 JSON.parse 白屏（P0-3）；fetchAdminInfo 死代码 |
| `src/api/index.ts` | 🟠 401 不区分登录失败/过期；outletLogin 与 outlet.ts 重复定义 |
| `src/router/index.ts` | 🟠 动态路由默认权限缺失（4.8） |

---

## 七、修复优先级建议

**P0（立即，功能/安全阻断）**
1. 修复 `OrderAssign.vue:359`、`bookkeeping.vue:274` 二次解包 → 恢复派单功能（1 处公共工具函数即可根治）。
2. 修复 `TrendChart.vue` 空数据初始化 → 趋势图恢复显示。
3. `auth.ts` JSON.parse 加 try/catch → 消除白屏风险。
4. `content/index.vue` 富文本净化（DOMPurify 或后端过滤）。
5. 补齐历史订单页操作按钮/分页，或明确降级只读并删死代码。
6. 修复 `OutletList.vue` 结算周期字段名（`settlement_cycle` → `settlementCycle`）。
7. 对齐 `OutletOverview.vue` 字段名（`totalOrders` ↔ `total_orders`）并推动后端补全聚合统计。

**P1（尽快）**
8. 清理 `detail.vue` 只读硬编码与发货 fetch 缺陷（重构或删除）。
9. 修复交易流水导出与订单跳转。
10. 网点工作台 3 个功能缺陷（业务类型空配置、100 条截断、绝对 URL）。
11. 修正 `OutletDashboard.vue` 状态机映射（3=已完成、4=已拒绝）与 `assigned_at`→`assignedAt`、消除挂载重复请求。
12. `seals.vue` 初始化重复请求；`scenes.vue`/`newspapers.vue` 初始化 catch。
13. 统一响应解包写法（消灭手工 `.data`）。

**P2（质量）**
14. 配置 ESLint + 统一 formatDate + 清理 console.log + 收敛 `any`。
15. 统一金额单位口径；`e !== 'cancel'` 补 `'close'`；按钮 loading 防重。
16. 提交未落库的改动（见下），修正 PM2 配置。

---

## 八、已核实为"误报/需澄清"的项（避免误导）

> 以下项在初查时被标记为疑点，经后端源码交叉核实后判定**非缺陷**或需澄清：

1. **`transaction/index.vue` 的 `resp.data` 二次取值** —— ✅ 与后端契约一致：`TransactionController` 每端点经 `inner()` 包装后再被 `ResponseWrapAdvice` 包一层，前端解一层后 `res` 即 `{data:...}`，`resp.data.x` 正确。风险点仅在：若后端未来统一为单层包装会立即失效，建议在 api 层固化。
2. **`scenes.vue` 使用 `p.sealIds`（非 seal_ids）** —— ✅ 后端 `SealService.packageToRawMap`（941 行）返回 `sealIds`（camelCase），前端用法正确；`packages.vue:69` 的 `seal_ids` 判断只是防御式兼容。原"字段名不一致导致清空关联"的判断不成立。
3. **`Workspace.vue` 轮询定时器泄漏** —— ✅ 已核实 `onUnmounted → stopPolling` 正确清理。
4. **`content/index.vue` 手动读 localStorage token** —— ✅ 必要：el-upload/wangEditor 走自身 XHR 不经 axios 拦截器；代价是绕过 401 统一处理（建议收敛为统一上传封装）。
5. **`dashboard/content/finance` 的 watch+immediate 与 onMounted 重复请求** —— ✅ 逐行核实不存在。
6. **金额单位"整仓放大 100 倍"** —— ❌ 不成立：订单 DTO 的 `totalPrice/payPrice/totalAmount` 全仓按"元"展示自洽；仅 `refund.refundFee`（remark 内嵌）按"分"处理，两处口径各自自洽，但**同文件内 payPrice 与 refundFee 单位不一致**仍是展示层风险（见 5.3），需与后端确认 payPrice 单位。
7. **`OrderAssign.vue` 的 `v-html` 高亮 XSS** —— ✅ 已核实 `highlight()` 对 `before/hit/after` 全部经 `escapeHtml` 转义（394-396 行），搜索词注入会被转义为纯文本，无注入面；维护时勿改回直接拼接。
8. **网点端/管理端 token 使用** —— ✅ 全部页面接口与 token 类型匹配正确（网点端走 outlet_token 专属接口，管理端走 admin_token），未发现网点端调用管理端接口或 token 错配。

---

## 九、环境与交付附注

- **未提交改动风险**：当前工作区有 **39 个文件、约 +1900/-971 行未提交改动**（含 content/index.vue +455、finance/index.vue -438 等大改），一旦误操作存在丢失风险，建议尽快提交或建立工作分支。
- **沙箱限制**：`vite build` 因 esbuild 子进程命名管道被安全沙箱拦截未能复验（非项目问题）；`vue-tsc` 已通过。
- 本报告所有行号以审计当日工作区代码为准。

---

*报告生成：2026-08-26 ｜ 基于静态审读、工具验证与后端源码契约交叉核对；建议修复后复跑一次 vue-tsc 与构建验证。*
