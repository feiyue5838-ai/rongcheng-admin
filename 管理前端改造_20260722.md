# 管理前端改造完成报告

**时间**: 2026-07-22 13:32
**任务**: 评价管理页面添加状态筛选 + 新建问答管理页面

---

## 一、评价管理页面改造

**文件**: `D:\rongcheng-admin\admin\src\views\reviews\index.vue`

### 新增功能

1. **状态筛选 Tabs**
   - 待审核（默认显示）
   - 已通过
   - 已驳回
   - 全部

2. **审核操作列**
   - 待审核状态显示「通过」「驳回」按钮
   - 调用 `PUT /api/reviews/:id/status` 接口

3. **状态标签显示**
   - 在表格中新增「状态」列，展示当前审核状态

### API 接口

```typescript
// 新增：审核评价
export const updateReviewStatus = (id: string, status: 'pending' | 'approved' | 'rejected') =>
  request.put(`/reviews/${id}/status`, { status })
```

---

## 二、问答管理页面（新建）

**文件**: `D:\rongcheng-admin\admin\src\views\questions\index.vue`

### 页面功能

1. **顶部筛选**
   - Tab 切换：待审核（默认）/ 已发布 / 已驳回 / 全部
   - 关键词搜索（问题内容）
   - 模块筛选：企业刻章 / 个人刻章 / 登报

2. **表格列**
   - 序号（带分页计算）
   - 问题内容（截断显示，悬停展开）
   - 模块（标签形式）
   - 提问者
   - 回复数（Badge 显示）
   - 状态（标签形式）
   - 发布时间
   - 操作：审核通过 / 驳回 / 回复 / 详情 / 删除

3. **回复弹窗**
   - 显示问题内容
   - 已有回复列表（支持删除回复）
   - 输入框提交新回复

4. **详情弹窗**
   - 完整问题内容
   - 所属模块
   - 提问者信息
   - 状态
   - 发布时间
   - 回复记录

### API 接口

```typescript
// 问答相关接口（新增）
export const getQuestions = (params?: any) => request.get('/questions/admin/list', { params })
export const updateQuestionStatus = (id: string, status: 'pending' | 'published' | 'rejected') =>
  request.put(`/questions/${id}/status`, { status })
export const replyQuestion = (id: string, content: string) =>
  request.post(`/questions/${id}/replies`, { content })
export const deleteQuestion = (id: string) => request.delete(`/questions/${id}`)
export const deleteQuestionReply = (replyId: string) => request.delete(`/questions/replies/${replyId}`)
```

---

## 三、路由配置

**文件**: `D:\rongcheng-admin\admin\src\router\index.ts`

```typescript
// 新增路由
{
  path: 'questions',
  name: 'Questions',
  component: () => import('@/views/questions/index.vue'),
  meta: { title: '问答管理' }
}
```

---

## 四、侧边栏菜单

**文件**: `D:\rongcheng-admin\admin\src\layout\index.vue`

在「评价管理」下方新增菜单项：

```html
<el-menu-item index="/questions">
  <el-icon><ChatLineRound /></el-icon>
  <span>问答管理</span>
</el-menu-item>
```

---

## 五、技术栈

- **UI 框架**: Element Plus（全局注册）
- **图标**: @element-plus/icons-vue（全局注册）
- **日期处理**: dayjs
- **状态管理**: Vue 3 Composition API + reactive/ref
- **HTTP 请求**: axios（封装在 `@/api`）

---

## 六、认证机制

所有管理端接口自动附加 `admin_token`：

```typescript
const adminToken = localStorage.getItem('admin_token')
if (adminToken) config.headers.Authorization = `Bearer ${adminToken}`
```

---

## 七、文件清单

| 文件路径 | 操作 | 说明 |
|---------|------|------|
| `src/api/index.ts` | 编辑 | 新增评价审核、问答管理接口 |
| `src/views/reviews/index.vue` | 重写 | 添加状态筛选和审核功能 |
| `src/views/questions/index.vue` | 新建 | 问答管理页面 |
| `src/router/index.ts` | 编辑 | 新增问答管理路由 |
| `src/layout/index.vue` | 编辑 | 新增侧边栏菜单项 |

---

## 八、待后端对接

后端需确保以下接口正常返回：

- `GET /api/reviews/admin/list` - 支持 `status` 参数筛选
- `PUT /api/reviews/:id/status` - 审核评价
- `GET /api/questions/admin/list` - 问答列表（支持 status/keyword/module 参数）
- `PUT /api/questions/:id/status` - 审核问答
- `POST /api/questions/:id/replies` - 回复问答
- `DELETE /api/questions/:id` - 删除问答
- `DELETE /api/questions/replies/:replyId` - 删除回复

---

**任务完成** ✅
