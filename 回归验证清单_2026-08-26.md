# 回归验证清单（按建议实施顺序）

> 对应：《前端审计修复方案_2026-08-26.md》第五节
> 状态说明：所有修复已于提交 `c3d7922` 落地（vue-tsc 0 错误、vite build 通过）。
> 本清单用于**运行时手工回归**：逐项操作 → 核对预期结果 → 打勾。
> 测试环境：后端 8080 运行中，前端 `npm run dev`（或预览 dist）。

---

## 阶段一：P0 高危（按建议顺序）

### P0-1 派单网点列表恒为空（最高优先）
- 操作：登录 → 进入「订单分配」`/outlets/assign` → 在待分配订单上点「分配」→ 打开分配弹窗
- 预期：弹窗内有**可选网点列表**（推荐网点置顶、支持关键词搜索过滤），不再是空列表
- 再验：历史记账订单列表 → 点「分配」→ 弹窗同样有网点
- 依赖：无
- [ ] 通过

### P0-3 刷新白屏（一行改动见效）
- 操作：浏览器 F12 → Application → Local Storage → 把 `admin_info` 值改成损坏 JSON（如 `{"name":`）→ 刷新页面
- 预期：不再白屏，正常回退到未登录/登录页；控制台无 JSON.parse 报错
- 恢复：删掉该 key 后重新登录
- [ ] 通过

### P0-9 网点结算周期设置失效
- 操作：「网点管理」→ 新增或编辑网点 → 结算周期选「周结」+ 每周几 → 保存
- 预期：保存成功；**重新打开编辑弹窗回显正确**（周期与起算日一致）；后端 DB `outlets.settlement_cycle` 有值
- 再验：清空结算周期保存 → 关闭自动结算生效（不报错）
- [ ] 通过

### P0-2 财务趋势图首次加载不渲染
- 操作：进入「财务中心」`/finance`（首次进入，数据异步到达）
- 预期：趋势图**首次进入即正常渲染**（不再停留在"暂无趋势数据"）
- 再验：切换 近7天/近30天/近90天 → 图表随数据刷新
- [ ] 通过

### P0-4 内容管理富文本存储型 XSS
- 操作：进入「内容管理」`/content` → 公告或关于我们 Tab → 打开富文本编辑器
- 预期 1：工具栏**没有"源码/HTML"按钮**（已禁用 editHtml）
- 预期 2：粘贴 `<script>alert(1)</script>` 与 `<img src=x onerror=alert(1)>` → 保存 → 重新打开，内容被剥离/净化，**无弹窗执行**
- 预期 3：正常图片/表格粘贴仍保留（白名单未过度收紧）
- [ ] 通过

### P0-5 历史订单操作无入口 + 记账缺分页
- 操作：
  - 历史刻章/登报订单列表 → 操作列出现「分配」（未分配订单）、「发货」（制作中订单）
  - 历史记账订单列表 → 操作列出现「标记支付/分配/完成/退款」（按状态）；**底部出现分页器**，可翻到第 2 页
  - 历史售后订单 → 操作列出现「确认退款 / 拒绝」
- 预期：各按钮触发对应弹窗且操作成功（分配后网点可见、发货后状态变更）
- 注意：按钮显示条件按状态机推断，若与业务流转不符请按后端状态常量（1待接单…9已退款）微调
- [ ] 通过

### P0-6 订单详情发货（axios 化）
- 操作：历史订单列表 → 对状态「制作中(3)」订单点「发货」→ 填快递公司/单号 → 提交
- 预期：成功提示；无 401/未授权；Network 面板只发一次 `PUT /api/orders/{id}/deliver-admin`（带 Authorization）；后端 DB `express_company/express_no` 正确落库，无重复发货
- [ ] 通过

### P0-7 交易流水导出 + 订单跳转
- 操作：进入「交易流水」`/finance?tab=transaction`
  - 点「导出」→ 下载 CSV → 用 Excel 打开，中文正常（带 BOM），表头与表格列一致
  - 点「关联订单」→ 新窗口打开对应订单详情 `/orders/:id`（**不再跳回工作台**）
  - 本地凌晨 0:00–7:59 时段点「今日/昨日」快捷筛选 → 日期正确（P1-6 一并验证）
- [ ] 通过

### P0-8 退款 V1 死页面删除
- 操作：直接访问 `/refund`、`/finance/refund`
- 预期：自动重定向到 `/v2/refunds`；控制台无 404；无残留页面入口
- [ ] 通过

### P0-10 全网点总览字段（前端已对齐，依赖后端）
- 操作：进入「全网点总览」`/outlets/overview`
- 预期：五个指标卡、四个 KPI、大区柱状图、两个表格渲染正常，无 NaN
- ⚠️ **后端依赖**：`OutletService.getOverview()` 当前订单统计为占位 0——前端字段已对齐，真实数据需后端补聚合后复验
- [ ] 通过（字段级） / [ ] 待后端聚合后复验

---

## 阶段二：P1

### P1-1 网点工作台（业务类型空配置 / 绝对URL / 100条）
- 操作：网点端登录 → `/Outlet/workspace`
  - 未配置业务类型的网点：能看到全部订单（不再整体置空），控制台有 warn 提示
  - 材料预览：URL 为绝对地址（https://...）时新窗口能正常打开
  - 订单 >100 条：页面顶部出现"仅显示最近 100 条"警告条
- [ ] 通过

### P1-2 网点看板（状态机 / assignedAt / 重复请求 / 跳转）
- 操作：「网点看板」`/outlets/dashboard`
  - 订单状态 3 显示「已完成」、4 显示「已拒绝」（不再 3=已发货/4=已完成）
  - 「已完成」统计数正确（按状态 3 统计）
  - 分配时间列有值（不再恒 "-"）
  - Network 面板：进入页面时 `/outlets/{id}` 与 `/outlets/{id}/orders` **各只请求一次**
  - 「查看全部订单/交付回执/网点列表」为 SPA 路由跳转（页面不整刷）
- [ ] 通过

### P1-3 印章管理初始化重复请求
- 操作：进入「产品管理 → 企业刻章」
- 预期：Network 面板初始**只按当前场景拉取**（不再先聚合 16 个请求再覆盖）；其他场景 Tab 角标计数正确显示；切 Tab 正常加载
- [ ] 通过

### P1-4 场景 / 报纸初始化兜底
- 操作：断网或让对应接口失败（如后端临时停掉）后进入「场景管理」「报纸仓库」
- 预期：页面**仍可用**（场景列表能加载/报纸列表能加载），不再整页空白卡死；恢复网络后正常
- [ ] 通过

### P1-5 响应解包统一（交易流水）
- 操作：交易流水页刷新
- 预期：列表/统计卡片/合作方下拉**数据正常**（api 层已解两层包装，页面不再手工 .data）；控制台无 undefined 报错
- [ ] 通过

### P1-6 交易流水时区（与 P0-7 一并）
- 操作：见 P0-7；凌晨时段「今日/昨日/近7天/近30天」日期正确
- [ ] 通过

---

## 阶段三：P2 质量

- [ ] 确认弹窗按 ESC / 右上角「×」关闭 → 不再误弹"操作失败/删除失败"（questions/reviews/users/OutletList）
- [ ] 列表翻到第 5 页后切换筛选/搜索 → 自动回到第 1 页（OrderAssign/OutletList/refund-records/after-sales/newspapers）
- [ ] 编辑/删除网点后停留在当前页（不再强制跳回第 1 页）
- [ ] 登录按钮连点 / 密码框回车 → 只提交一次（login、outlet Login）
- [ ] 积分奖品「保存」按钮有 loading、连点不重复创建
- [ ] 问答/管理员列表日期为空时显示 `-`（不再 Invalid Date）
- [ ] 控制台无 `[ModuleDonut]/[TrendChart]/[outlets]` 调试日志

---

## 阶段四：全局收尾

- [ ] `npx vue-tsc --noEmit` → 0 错误
- [ ] `powershell -ExecutionPolicy Bypass -File scripts\ci-check.ps1` → CI PASS（ESLint 未装则跳过提示）
- [ ] 登录 / 登出 / 各角色权限跳转正常（403 页）
- [ ] 各模块 CRUD 抽查：网点、印章、报纸、场景、内容、用户、评价、问答、FAQ、积分
- [ ] 页面无 500/404 控制台报错

---

*生成：2026-08-26 ｜ 配套提交 c3d7922 / 9c174ea / ec57b9e*
