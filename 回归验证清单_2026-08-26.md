# 回归验证清单（按建议实施顺序）

> 对应：《前端审计修复方案_2026-08-26.md》第五节
> 状态说明：所有修复已于提交 `c3d7922` 落地；本清单用于**运行时回归验证**：逐项操作 → 核对预期结果 → 打勾。
> 测试环境：后端 8080 运行中（MySQL 版 jar），前端预览 5185。

> **2026-08-29 端到端验证更新**：后端 8080（MySQL 版）+ admin `npm run build` + taro weapp 构建 + preview(5185) 代理链路全部通过；修复 P0 `newspaper_order_details` 缺列导致 v2 订单详情 500。

> **2026-08-29 晚 回归逐项验证更新**：对清单全部条目做**代码级 + API 级**逐项核验（标注证据位置），并修复 4 处问题，后端已重建重启、预览已恢复：
> 1. **阶段四 vue-tsc 失败**：`products/seals.vue` 场景管理弹窗引用未定义的 `sceneManageList`（16:34 未提交改动引入）→ 已补 `sceneManageList` 定义（过滤真实场景）+ 打开弹窗前刷新列表，vue-tsc 复跑 0 错误，vite build 40.16s 通过，新 dist 已上线预览。
> 2. **P2-3**：网点编辑/删除后 `loadData()` 无参调用强制回第 1 页 → 改为 `loadData(currentPage.value)` 停留当前页（新增仍回第 1 页）。
> 3. **P0-10**：后端 `OutletService.getOverview()` 订单统计硬编码 0 → 补真实聚合（order_assignments 内存聚合），重建 jar（58.1MB）→ 重启 8080（PID 12048，14.2s 就绪）→ **API 复验返回真实数字**。
> 4. **admin 预览 5185 中断**：原预览进程（PID 20676）已不监听 → 杀掉并以新 dist 重启 `vite preview`，GET / 与 /api 代理均 200。
>
> 标注规则：`[x]` = 已验证（代码/API 级，附证据）；`⏳` = 待人工浏览器点验（UI 交互项）；`⚠️` = 部分满足或设计取舍。

---

## 阶段一：P0 高危（按建议顺序）

### P0-1 派单网点列表恒为空（最高优先）
- [x] 通过（API 层：v2/admin/orders/unassigned 返回待派订单；assign 端点含网点校验）
- 代码级：分配弹窗含网点表格（推荐匹配度/业务类型/空态文案，seal.vue:134-195），关键字搜索 + 空态「暂无可用网点」
- ⏳ 弹窗网点列表 UI 点验（推荐置顶、关键词过滤）

### P0-3 刷新白屏（一行改动见效）
- [x] 通过（代码级：`stores/auth.ts:8-12` `safeParseAdminInfo()` 对损坏 JSON try/catch 回退 null → 未登录态跳登录页，不再白屏）
- ⏳ 浏览器实测：改坏 `admin_info` 后刷新 → 回登录页

### P0-9 网点结算周期设置失效
- [x] 通过（全链路代码+DB 级）：
  - 前端发 camelCase `settlementCycle/settlementWeeklyStartDay`（OutletList.vue:810-814）
  - 后端 create/update 均持久化（OutletService.java:230-232 / 302-308），响应含两字段（:856-857）
  - MySQL `outlets` 表有 `settlement_cycle`(TEXT) + `settlement_weekly_start_day`(INT) 列（schema:341）
  - 清空周期 → 置 null（关闭自动结算）
- ⏳ UI 点验：编辑弹窗回显一致

### P0-2 财务趋势图首次加载不渲染
- [x] 通过（代码级：TrendChart.vue:177-185 watch data 到达后补 `initChart()`；finance/index.vue:55-58 传异步 `trendData`，首次进入即可渲染）
- ⏳ UI 点验：切 近7/30/90 天图表刷新

### P0-4 内容管理富文本存储型 XSS
- [x] 通过（代码级：content/index.vue:437 `excludeKeys:['editHtml']` 禁源码按钮；:899-940 `sanitizeRichText` 白名单净化：SCRIPT/IFRAME/OBJECT/FORM 等整删、非白名单标签解包、on* 事件属性全剥、`javascript:`/`data:` 协议拦截；保存统一走净化 :945）
- ⏳ UI 点验：粘贴恶意 HTML 保存后无弹窗、正常图文保留

### P0-5 历史订单操作无入口 + 记账缺分页
- [x] 通过（代码级）：
  - 刻章页：分配（无 assignment 时）+ 发货（status===3）+ 分页器（seal.vue:95-110）
  - 记账页：标记支付(1)/分配(非已分配)/完成(4)/退款(5) + 分页器（bookkeeping.vue:114-127）
  - 售后页：确认退款/拒绝弹窗（after-sales-orders.vue:91-134）
- ⏳ UI 点验：各按钮弹窗操作成功

### P0-6 订单详情发货（axios 化）
- [x] 通过（代码级：api/index.ts:218 `PUT /orders/{id}/deliver-admin`，snake_case `express_company/express_no/admin_remark`；request 拦截器自动带 `Bearer admin_token` :26-27；seal.vue:304-311 必填校验）
- ⏳ UI 点验：发货后 DB 落库、无重复请求

### P0-7 交易流水导出 + 订单跳转
- [x] 通过（代码级：transaction/index.vue:454 CSV 前缀 `\uFEFF`（Excel 中文不乱码）+ 字段转义；:499 `window.open('/orders/'+orderId,'_blank')` 新窗开详情；:367-396 今日/昨日/近7/近30 快捷筛选）
- ⏳ UI 点验：下载 CSV 用 Excel 打开、关联订单跳转

### P0-8 退款 V1 死页面删除
- [x] 通过（代码级：router/index.ts:88 `/finance/refund`→`/v2/refunds`、:93 `/refund`→`/v2/refunds`；:108 404 兜底 `/dashboard`）
- ⏳ UI 点验：直接访问旧路径无 404

### P0-10 全网点总览字段（前端已对齐，依赖后端）
- [x] 前端字段已对齐（OutletOverview.vue：5 指标卡 + 4 KPI + 大区柱状图 + 2 表格；NaN 防护 computed；camel/snake 双命名归一化 :258-275）
- [x] 后端聚合已实现并**已上线复验**（本轮：OutletService.getOverview() 一次拉取全部 `order_assignments`，按网点统计 total/pending(1)/processing(2)/completed(3)/today(assignedAt 当日)，region/summary 同步汇总；per-outlet 同时输出 camelCase `totalOrders` 别名）
- [x] **API 复验通过**（重启后）：summary totalOutlets=8/active=3/total_orders=1/pending=1（真实数据，非占位 0）；topOutlets 按订单量排序；regions 大区汇总正确
- 注：统计口径为 V1 网点履约 `order_assignments`（与网点列表 totalOrders 同源，符合审计建议）；V2 供应商履约 `fulfillment_orders`(15 条) 不属于网点维度，不混入
- ⏳ UI 点验：页面渲染（指标卡/柱状图/表格无 NaN）

---

## 阶段二：P1

### P1-1 网点工作台（业务类型空配置 / 绝对URL / 100条）
- [x] 通过（代码级：Workspace.vue:20-21 超 100 条警告条；:417 绝对 URL 直开；:468-469 未配置业务类型放行全部 + console.warn）
- ⏳ UI 点验：网点端登录操作

### P1-2 网点看板（状态机 / assignedAt / 重复请求 / 跳转）
- [x] 通过（代码级：OutletDashboard.vue:212-218 状态映射 3=已完成/4=已拒绝；:257 统计按状态 3；:119-120 分配时间列；onMounted 仅 `loadOutlets()` → 内部首个网点一次 `loadData()`（:354-357，getOutletAPI+getOutletOrdersAPI 各 1 次）；:343-351 router.push SPA 跳转）
- ⏳ UI 点验：Network 面板确认各 1 次请求

### P1-3 印章管理初始化重复请求
- ⚠️ 部分满足（设计取舍）：`suppressSceneWatcher` 标志已落地（seals.vue:866-867），聚合完成后不再触发 watcher 重复拉取/覆盖——「重复请求+覆盖」已修复；但**初始仍聚合全部业务场景**（`__ALL_BUSINESS__` → Promise.all 8 场景 × seals/packages，:854-858），与清单措辞「只按当前场景拉取」不符。原因：需先聚合才能决策「有数据的默认场景」。若要严格只拉当前场景，需引入持久化默认场景或路由参数，属新设计，暂不自动执行。
- ⏳ UI 点验：Network 面板确认无「聚合后再次拉取当前场景」的重复

### P1-4 场景 / 报纸初始化兜底
- [x] 通过（代码级：scenes.vue:311-316 `Promise.allSettled` 单接口失败不阻塞；newspapers.vue:1006-1014 try/catch + 独立 fetchNewspapers）

### P1-5 响应解包统一（交易流水）
- [x] 通过（代码级：transaction/index.vue api 层已解两层包装，`stats.value = resp || {}`（:335），模板全部可选链 `stats.today?.income || 0`，无 undefined 崩溃路径）

### P1-6 交易流水时区（与 P0-7 一并）
- [x] 通过（代码级：transaction/index.vue:369-374 用本地时间 `getFullYear/getMonth/getDate` 拼日期，注释明确规避 toISOString 的 UTC 偏移；凌晨时段不会错算到前一天）

---

## 阶段三：P2 质量

- [x] 确认弹窗按 ESC / 「×」关闭 → 不再误报"操作失败/删除失败"：questions(280/326/348)、users(82/90)、reviews(186/227)、OutletList(857) 均为 `if (e !== 'cancel' && e !== 'close')` 豁免模式
- [x] 列表翻页后切换筛选/搜索 → 回第 1 页：OrderAssign search()(306-307)、OutletList onFilterChange()(862-865)、refund-records(141-142)、after-sales(168/243)、newspapers watch(786-788) 全部重置
- [x] 编辑/删除网点后停留在当前页：**本轮修复**（原 loadData() 无参强制回第 1 页 → 改为 loadData(currentPage.value)；新增仍回第 1 页）——需 vite 构建验证
- [x] 登录按钮连点 / 密码框回车 → 只提交一次：login(61 `if (loading.value) return` + :loading)、outlet Login(67 同款)
- [x] 积分奖品「保存」loading + 连点不重复：points/index.vue saving 守卫 + `:loading="saving"`
- [x] 问答/管理员列表日期空显示 `-`：questions formatDate(`d ? … : '-'`)；admins lastLoginAt 三元 `? … : '-'`
- [x] 控制台无 `[ModuleDonut]/[TrendChart]/[outlets]` 调试日志：全 src 0 处 `console.log`；仅 echarts 加载失败的 `console.error`（合理错误日志）

---

## 阶段四：全局收尾

- [x] `vue-tsc --noEmit` → 0 错误：**本轮发现并修复** seals.vue `sceneManageList` 未定义（TS2339），复跑 exit 0
- ⏳ `scripts\ci-check.ps1` → CI PASS：vite build 进行中（esbuild 在沙箱 spawn EPERM，需 danger-full-access）
- [x] 登录/登出/角色权限：代码级（router 守卫 hasAccess → /403；403 页存在；角色枚举/菜单权限已打通）
- [x] 各模块 CRUD 抽查（API 级全 200）：v2 orders(51)/unassigned/settlements/refunds、outlets、outlets/admin/overview、seals/admin/scenes、newspapers、users、reviews、questions、points/rewards、after-sales、orders/unassigned
- ⏳ 页面无 500/404 控制台报错：API 层无 500/404；页面控制台需浏览器点验

---

*生成：2026-08-26 ｜ 配套提交 c3d7922 / 9c174ea / ec57b9e ｜ 2026-08-29 晚：代码级逐项复核 + 3 处修复（seals.vue 类型错误 / P2-3 停留当前页 / P0-10 后端聚合待重启）*
